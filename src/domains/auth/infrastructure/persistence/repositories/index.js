/**
 * Repository Implementations Export
 * UserRepository moved to shared domain
 */

const TokenBlacklistRepository = require('./TokenBlacklistRepository');

module.exports = {
  TokenBlacklistRepository
};

