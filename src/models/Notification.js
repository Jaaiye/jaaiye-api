/**
 * Legacy Notification Model
 * Re-exports the DDD schema to prevent duplicate model registration
 */
module.exports = require('../domains/notification/infrastructure/persistence/schemas/Notification.schema');