/**
 * DTOs Export
 */

const RegisterDTO = require('./RegisterDTO');
const LoginDTO = require('./LoginDTO');
const GoogleOAuthDTO = require('./GoogleOAuthDTO');
const AppleOAuthDTO = require('./AppleOAuthDTO');

module.exports = {
  RegisterDTO,
  LoginDTO,
  GoogleOAuthDTO,
  AppleOAuthDTO
};

