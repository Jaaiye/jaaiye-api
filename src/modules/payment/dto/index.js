const InitializePaymentDTO = require('./InitializePaymentDTO');
const RegisterTransactionDTO = require('./RegisterTransactionDTO');
const UpdateTransactionDTO = require('./UpdateTransactionDTO');

module.exports = {
  InitializePaymentDTO,
  RegisterTransactionDTO,
  UpdateTransactionDTO
};

