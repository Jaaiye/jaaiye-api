/**
 * Referral Repositories Export
 */

const ReferralRepository = require('./ReferralRepository');
const ReferralTrackingRepository = require('./ReferralTrackingRepository');

module.exports = {
    ReferralRepository,
    ReferralTrackingRepository
};
